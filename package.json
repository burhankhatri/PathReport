{
  "name": "medical-transcription-system",
  "version": "1.0.0",
  "description": "Medical observation transcription and report generation system",
  "main": "server/index.js",
  "scripts": {
    "server": "node server/index.js",
    "client": "cd client && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "install-all": "npm install && cd client && npm install",
    "vercel-build": "cd client && npm install && npm run build"
  },
  "keywords": [
    "medical",
    "transcription",
    "ai",
    "deepgram",
    "claude"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.27.0",
    "@deepgram/sdk": "^3.4.0",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "multiparty": "^4.2.3",
    "pdfkit": "^0.14.0",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
